spring:
  data:
    mongodb:
      uri: mongodb://root:123456@127.0.0.1:27017/stars_task
  # 线程池配置
  task:
    pool:
      corePoolSize: 4
      maxPoolSize: 8
      keepAliveSeconds: 60
      queueCapacity: 32 # 这个数值根据并发亮的大小来设置
eureka:
  client:
    service-url:
      # 指定eureka server通信地址，注意/eureka/小尾巴不能少
      defaultZone: http://wp:wp123@localhost:8000/eureka/
  instance:
    # 是否注册IP到eureka server，如不指定或设为false，那就会注册主机名到eureka server
    prefer-ip-address: true
# ===================================================================
# 分布式文件系统FDFS配置
# ===================================================================
fdfs:
  so-timeout: 6000          # 3秒超时
  connect-timeout: 1000     # 1秒超时
  thumb-image:              # 缩略图生成参数
    width: 150
    height: 150
  pool:
    max-wait-millis: 30000
  tracker-list:             # TrackerList参数,支持多个
#    - 192.168.100.122:22122
#    - 192.168.50.100:22122
    - 192.168.101.106:22122
#  webServerUrl: http://192.168.50.100/
  webServerUrl: http://192.168.0.121/

#logging:
#  level:
#    com.github.tobato.fastdfs.domain.conn: DEBUG
